<div>
  <p>Jogo do dia: <b><%= Game.last.name %></b></p>

  <p>OpenAI: <b><%= OpenAI::Client.new.completions(
      parameters: {
        model: "text-davinci-003",
        prompt: 'Responda com o nome de um jogo aleatório para PC, Playstation, Xbox ou Nintendo Switch. Exemplo: "Baldur\'s Gate 3"',
        max_tokens: 1000,
        temperature: 0.5,
        top_p: 1,
        frequency_penalty: 0,
        presence_penalty: 0
      }
  ) %></b></p>
</div>

<div class="mt-10">
  Jogos anteriores:

  <ul class="list-disc">
    <% Game.all.each do |game| %>
      <li class="ml-5"><%= "#{game.name} (#{game.created_at.to_formatted_s(:long_ordinal)})" %></li>
    <% end %>
  </ul>
</div>
